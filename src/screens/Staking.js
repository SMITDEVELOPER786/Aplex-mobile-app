import React from 'react';
import CryptoBrowseHub from '../components/CryptoBrowseHub';

const Staking = ({ navigation }) => (
    <CryptoBrowseHub navigation={navigation} showBack />
);

export default Staking;
