import React from 'react';
import CryptoBrowseHub from '../components/CryptoBrowseHub';

const Discover = ({ navigation }) => (
    <CryptoBrowseHub navigation={navigation} />
);

export default Discover;
